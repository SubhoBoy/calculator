#include "input.h"

// The setup function is called once when the sketch starts
void setup() {
  // Initialize the input system
  input_setup();
}

// The loop function is called repeatedly
void loop() {
  // Run the input system's main loop
  input_loop();
}
